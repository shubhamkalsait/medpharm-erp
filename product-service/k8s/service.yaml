apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  type: ClusterIP   
  ports:
  - port: 8082
    targetPort: 8082
